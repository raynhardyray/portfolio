---
import { Icon } from "astro-icon/components";

type AboutListProps = {
    iconName?: string;
    label?: string;
    isLink?: boolean;
    href?: string;
    target?: boolean;
};

const {
    iconName = "",
    label = "",
    isLink = false,
    href = undefined,
    target = false,
}: AboutListProps = Astro.props
---
<div class="flex gap-2">
    <Icon name={iconName} class="w-8 h-8 shrink-0" />
    {isLink ? (
        <a
            href={href}
            target = {target ? "_blank" : undefined}
            rel={target ? "noopener noreferrer" : undefined}
            class="text-xl underline underline-offset-5 break-all"
        >{label}</a>
    ) : (
        <p class="text-xl">{label}</p>
    )}
</div>